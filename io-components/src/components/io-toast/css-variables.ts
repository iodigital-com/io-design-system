/**
 * Typed CSS custom property name constants for io-toast.
 *
 * These are the public override tokens exposed by io-toast.
 * Reference these constants in stylesheets and documentation
 * instead of string literals — rename-safe and auto-documented.
 *
 * Classification: public-api
 * All changes to these names are breaking changes (semver major).
 */

/** Maximum width of the toast container. Default: 400px. */
export const cssVarToastMaxWidth = '--io-toast-max-width' as const;
