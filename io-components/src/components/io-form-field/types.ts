/**
 * Named slot for io-form-field.
 *
 * `io-form-field` exposes a single default slot for the slotted form control
 * (io-input, io-select, io-textarea, io-checkbox, or io-radio).
 * The label, helper text, and validation message are controlled via props.
 *
 * Using the empty string `''` selects the default (unnamed) slot.
 */
export type IoFormFieldSlotName = '';
