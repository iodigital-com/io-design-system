---
'@iodigital-com/components': minor
---

feat(io-drawer): add `dismissButton` prop, fix `dismiss` event to fire only on user-initiated closes, add dev-time console.error when no accessible label is supplied
