/**
 * Horizontal alignment of the label relative to any icon.
 * 'start' — icon on the left; 'end' — icon on the right.
 */
export type IoButtonPureAlignLabel = 'start' | 'end';

/** Native button type attribute values */
export type IoButtonPureType = 'button' | 'submit' | 'reset';
