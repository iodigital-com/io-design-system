/**
 * io-fieldset types
 * =================
 * Minimal — io-fieldset has no variant unions.
 * The `aria` prop accepts an arbitrary key/value record.
 */

export type IoFieldsetAriaRecord = Record<string, string>;
