export type IoProgressColor = 'blue' | 'orange' | 'success' | 'warning' | 'error';
export type IoProgressSize = 'sm' | 'md' | 'lg';

/**
 * Numeric range: defaults to [0, 100].
 * @remarks Used to calculate aria-valuemin and aria-valuemax.
 */
export type IoProgressMinMax = number;
