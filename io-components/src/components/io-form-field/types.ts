/**
 * Type unions for io-form-field props.
 *
 * io-form-field currently uses primitive props only (string, boolean).
 * This file is a placeholder that satisfies the stable-promotion requirement
 * (#374) and provides a single extension point for future prop type unions
 * (e.g. IoFormFieldLayout, IoFormFieldSize) without touching the component class.
 */

// No union props exist on io-form-field yet.
// Add named type exports here as new @Prop() unions are introduced.
