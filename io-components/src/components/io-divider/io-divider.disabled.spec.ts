/**
 * io-divider has no `disabled` prop and no state to disable.
 * It is a purely decorative/structural separator.
 * This file exists to satisfy the project's per-component spec convention.
 * See io-divider.spec.ts for full render and prop coverage.
 */
import { describe, it } from 'vitest';

describe('io-divider — disabled state (N/A)', () => {
  it('has no disabled prop — no disabled-state tests required', () => {
    // io-divider does not have a disabled state; nothing to verify here.
  });
});
