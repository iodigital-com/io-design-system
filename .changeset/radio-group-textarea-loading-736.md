---
"@iodigital-com/components": minor
---

feat(io-radio-group, io-textarea): add loading prop with spinner overlay

loading: boolean (default false) — shows io-spinner overlay, sets aria-busy, blocks interaction.
