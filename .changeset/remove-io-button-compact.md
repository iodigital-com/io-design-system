---
"@iodigital-com/components": minor
---

Remove `compact` prop from `io-button`. The compact density variant is not part of the iO button specification. Use the `size` prop (`sm`, `md`, `lg`) for button density control.
